# Docker image (set by CI, or override locally)
DOCKER_IMAGE=username/406-records:latest

# Port to expose on host
LISTEN_PORT=3000

# Postmark email service
POSTMARK_TOKEN=your-postmark-server-token
FROM_EMAIL=noreply@406records.com
TO_EMAIL=contact@406records.com

# Cloudflare Turnstile (bot protection)
TURNSTILE_SECRET=your-turnstile-secret-key

# CORS allowed origin
ALLOWED_ORIGIN=https://www.406records.com
