# Docker image (set by CI, or override locally)
DOCKER_IMAGE=username/406-records:latest

# Port to expose on host
LISTEN_PORT=3000

# Directus CMS
PUBLIC_DIRECTUS_ENDPOINT=https://your-directus-instance.com

# Postmark email service
POSTMARK_API_TOKEN=your-postmark-token
FROM_EMAIL=noreply@example.com
CONTACT_FORM_EMAIL=contact@example.com
